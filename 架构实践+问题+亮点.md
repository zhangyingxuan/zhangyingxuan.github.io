亮点

- 微前端架构，子应用切换，公共依赖，合并部署
- SEO、性能优化
- 大数据表格渲染优化：分页、虚拟列表、canvas、canvas(canvas.transferControlToOffscreen())+web worker、skia+webasymbly

问题

- 长页面渲染卡顿：virtual-ref 减少 dom 数（1.6w->8k)、虚拟滚动
- 循环依赖：**定位**： 在 vite.config.ts 中集成 vite-plugin-circular-dependency。**分析**：根据结果分析报错；解决方法：解耦 A<->B => A->C,B-C; **解决**：将“加载时依赖”转变为“运行时依赖”。利用 import() 异步加载；解耦与重构，提取公共部分为独立模块。
